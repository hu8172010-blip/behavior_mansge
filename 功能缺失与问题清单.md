# 功能缺失与问题清单

> 以下按实际已交付页面逐模块列出"流程图有设计但前端未实现 / 不完善"的部分及细节问题。
> 已完成项用 ✅ 标记，待办项保留原始描述。

---

## 一、总览看板 `/dashboard`

### 流程图对照
总览看板业务流程图规划了三大看板模块：

| 看板 | 流程图设计 | 前端现状 | 缺失 |
|------|-----------|----------|------|
| **告警统计模块** | 告警数量、告警类型、趋势图表 | ✅ 有今日事件数/累计/待确认预警/活跃轨迹 | 无"按时间范围筛选"、无"告警级别分布饼图"、无"告警类型 TOP 排行" |
| **重点区域热力模块** | 地图展示人员聚集、高频异常区域 | ❌ **完全未实现** | 整个地图/热力区域模块缺失 |
| **设备健康一览模块** | 摄像头在线/离线/流量、设备故障统计 | ✅ 有在线率/健康度分布/设备总数 | 无"流量统计"、无"故障率趋势"、无"按区域筛选设备" |

### 细节问题
- ✅ ~~看板卡片无"点击跳转详情"能力~~ **P3 已完成**：四个统计卡片均可点击跳转对应模块
- 趋势图仅展示当日 24 小时柱状图，**无法切换日期范围**（周/月/自定义区间）。
- "实时同步"标签仅文字说明，无实际 WebSocket 或轮询状态指示。

---

## 二、实时轨迹管理 `/track/realtime`

### 流程图对照
轨迹管理业务流程图 → 分支 1「实时轨迹管理（实时监控/追踪）」

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入实时轨迹管理页面 | ✅ | ✅ | — |
| 处理流程分支 | 接入视频流（多摄像头/多区域） | ❌ **无视频画面** | 仅列表，无视频预览窗口 |
| 目标检测与跟踪（实时识别目标） | ✅ API 返回人员特征 | ✅ 展示人员特征 | — |
| 跨摄像头关联（识别同一目标） | ✅ `device_route` 路径串 | ✅ 展示路径 | — |
| 轨迹生成与展示（实时轨迹展示） | ✅ 列表+链路详情弹窗 | ✅ | — |
| 轨迹存储（实时轨迹数据存储） | 后端 | — | — |
| 轨迹持续更新（实时刷新轨迹） | 每 30 秒刷新 | ⚠️ 有但粗糙 | 见下方细节 |

### 细节问题
- **无视频画面接入**：流程图明确"接入视频流"，前端纯表格，看不到任何摄像头画面。
- **无"暂停自动刷新"开关**：强制每 30 秒整表重渲染，排查某条轨迹时无法暂停。
- **整表重渲染闪烁**：每次全量拉取 `size:50`，数据多时体验差；无增量更新机制。
- **关键字查询与自动刷新冲突**：输入关键词后 30 秒一到自动刷新会把搜索态冲掉（页码重置到第 1 页）。
- **"已持续"时长计算从 `chain_start_time` 到当前时间**，若后端时钟与前端不一致会偏差大；无服务端校准。
- 链路详情弹窗（`TrackDetailModal`）只展示摄像头途经明细，**未回链对应异常行为记录**（该人为什么被标记？触发了什么告警？）。
- 无"手动归档"入口（流程图有归档节点，前端无触发按钮）。

---

## 三、事后轨迹查询 `/track/history`

### 流程图对照
轨迹管理业务流程图 → 分支 2「事后轨迹查询（历史回溯）」

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入事后轨迹查询页面 | ✅ | ✅ | — |
| 设置查询条件（投入/区域/时间等） | ✅ 筛选器 | ⚠️ 有但不完整 | 缺区域筛选 |
| 条件检索（从数据库查询） | ✅ API | ✅ | — |
| 轨迹数据检索 | ✅ | ✅ | — |
| 轨迹拼接与生成（摄像头拼接） | ✅ | ✅ 展示路径串 | — |
| 轨迹回放与展示（地图/时间轴展示） | ❌ **应有回放能力** | 仅静态列表 | 无时间轴回放、无地图连线 |
| 结果分析与导出（分析/导出报表） | ❌ | ✅ **P3 已完成** | 已支持 CSV 导出 |

### 细节问题
- ✅ ~~无时间范围选择器~~ **P3 已完成**：已有开始/结束日期时间选择器
- **无区域筛选**：`region_code` 字段存在但表单未暴露。
- ✅ ~~无导出功能~~ **P3 已完成**：工具栏已添加"导出 CSV"按钮
- **无轨迹回放/地图可视化**：历史轨迹应以时间轴或地图形式回放人员移动路径，当前仅为静态表格。
- 该页复用了实时轨迹的 `HistoryTrackView` 组件，**与实时轨迹页面 UI 高度雷同**，缺少历史视角特有的聚合统计。

---

## 四、设备列表管理 `/device/list`

### 流程图对照
设备管理业务流程图 → 五大操作分支

| 操作类型 | 流程图设计 | 前端现状 | 缺失 |
|----------|-----------|----------|------|
| **新增设备** | 填写信息→校验→测试连接→保存→启动监测 | ✅ 有新增弹窗 | 无"测试设备连接与视频画面"步骤 |
| **状态查看** | 筛选→查看在线/画面/清晰度/流量/健康度 | ⚠️ 部分 | 健康/画质/流量字段不展示 |
| **远程控制** | 选择设备→控制动作(重启/开画面/调角度)→检查权限→确认下发 | ❌ **完全缺失** | 无任何控制入口 |
| **编辑/启停/删除** | 选择操作→执行 | ⚠️ 有编辑+启停，无删除 | — |
| **故障处置** | 心跳超时/画面异常→创建记录→尝试恢复→正常?→派单维修→复检通过? | ⚠️ 仅"关闭故障" | 缺派单/维修/复检全链路 |

### 细节问题
- **故障处置闭环断裂**：只有"关闭故障"按钮，无派单、无维修结果回填、无复检通过/失败判定；`fault_record.assigned_to`/`repair_result`/`repair_remark` 字段前端完全未消费。
- **关闭故障无二次确认弹窗**，重要操作易误触且不可逆。
- **`region_code`（区域编码）表单未暴露**：新增/编辑只有 `region_name` 文本框，编码永远为空——而"按辖区查看"的权限隔离依赖它，等于辖区隔离失效。
- **新增设备表单漏了"端口号"输入项**（`port` 字段取不到值）。
- **健康分/画面质量/通道数/能力集既不展示也不维护**：`health_score`/`video_quality`/`capability` 等字段在前端完全隐形。
- **故障处置状态枚举与数据库不一致**：前端选项缺"维修中(REPAIRING)"、无"已升级(UPGRADED)"，用 `REPAIRED` 替代，导致无法按"维修中"筛选、未知状态显示原始英文（如 `MEDIUM` 直接展示）。
- **设备状态与故障状态无联动**：关闭故障单后设备台账 `status` 不会自动回 `ONLINE`。
- **无删除设备入口**（仅有停用），与物理删除策略矛盾。
- **新增设备编号无格式提示/唯一性校验**（如 `CAM-xxx` 格式），靠后端报错才知重复。
- ✅ ~~设备与行为无联动~~ **P3 已完成**：设备台账新增"行为"按钮，可查看该设备最近行为记录

---

## 五、异常行为记录 `/data/abnormal-record`

### 流程图对照
数据管理模块流程图 → 异常行为记录分支

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入异常行为记录页面 | ✅ | ✅ | — |
| 选择操作类型（查询/导出） | ✅ 有查询 | ✅ **P3 已完成** | 已支持导出 |
| 设置查询条件（时间/区域/人员/行为类型） | ✅ 筛选器齐全 | ✅ **P3 已增强** | 新增 `camera_id` 设备筛选 |
| 系统检索异常行为记录 | ✅ | ✅ | — |
| 展示异常行为列表与详情 | ✅ 表格 | ✅ | — |
| 选择导出范围与格式 | ❌ | ✅ **P3 已完成** | 已支持按筛选条件导出 CSV |
| 生成并下载异常行为报表 | ❌ | ✅ **P3 已完成** | — |

### 细节问题
- ✅ ~~无数据导出功能~~ **P3 已完成**：工具栏已添加"导出 CSV"按钮，支持按当前筛选条件导出
- **无批量操作**（如批量确认/批量忽略/批量标记关注）。
- **置信度显示为原始小数**（如 `0.92`），未做百分比格式化（实时轨迹页做了 `%` 格式化，此页未统一）。
- ✅ ~~点击行无"跳转关联轨迹/告警"~~ **P3 已完成**：每行支持"查看轨迹"和"查看人员"弹窗

---

## 六、轨迹记录 `/data/track-record`

### 流程图对照
同上，数据管理 → 轨迹记录分支

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入轨迹记录页面 | ✅ | ✅ | — |
| 设置查询条件（人员/时间/区域） | ✅ | ⚠️ 缺区域 | — |
| 检索轨迹数据 | ✅ | ✅ | — |
| 加载对应视频与轨迹数据 | ❌ | ❌ | 无视频加载 |
| 播放轨迹回放 | ❌ | ❌ | 无回放 |
| 结果分析与导出 | ❌ | ✅ **P3 已完成** | 已支持 CSV 导出 |

### 细节问题
- ✅ ~~无导出功能~~ **P3 已完成**：工具栏已添加"导出 CSV"按钮
- **无区域筛选**（`region_code` 未暴露）。
- **该页与"事后轨迹查询"(`/track/history`) 功能高度重叠**：两处都能查历史轨迹、都展示链路列表+详情弹窗，但入口不同、筛选条件略有差异，容易让用户困惑"到底去哪查轨迹"。
- **无视频回放/地图可视化**（同历史轨迹页）。

---

## 七、用户资料管理 `/data/user-profile`

### 流程图对照
数据管理模块流程图 → 用户资料分支

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入用户资料管理页面 | ✅ | ✅ | — |
| 选择操作类型（查询/维护） | ✅ 有查询+标记关注 | ⚠️ 维护=标记关注 | 编辑用户资料？ |
| 校验数据完整性 | — | — | 无前端校验规则 |
| 查询用户资料列表 | ✅ | ✅ | — |
| 记录资料变更日志 | ✅ 有变更日志概念 | ⚠️ 仅"保存更新后的用户资料"文字 | 实际无独立变更审计视图 |

### 细节问题
- ✅ ~~无法查看人员完整档案和关联行为~~ **P3 已完成**：新增 `PersonDetailModal` 人员详情弹窗，展示档案 + 最近 20 条行为记录
- **重点关注无独立名单管理页**：仅有逐条"标记关注"按钮，无"重点关注人员名单"专用视图、无审核流程、无批量解除/批量加入。
- **匹配账号列显示"未匹配"时无"手动关联"入口**：`dm_anonymous_person.matched_user_id` 为空时应允许管理员手动绑定到已知账号。
- **无"编辑用户资料"功能**（如修正外形描述、调整风险等级），只能标记关注/取消。
- **无员工档案表支撑**："员工本人"角色需看到"本人授权状态和个人报表"，但系统无员工表，账号↔员工映射悬空。

---

## 八、操作日志 `/data/operation-log`

### 流程图对照
数据管理 → 操作日志分支（相对完整）

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 进入操作日志页面 | ✅ | ✅ | — |
| 选择操作类型（查询/导出） | ✅ 两者都有 | ✅ | — |
| 设置查询条件 | ✅ 模块/操作/时间/关键字 | ✅ | — |
| 检索操作日志 | ✅ | ✅ | — |
| 展示日志列表 | ✅ | ✅ | — |
| 选择导出范围与格式 | ✅ CSV 导出 | ✅ | — |
| 生成并下载 | ✅ | ✅ | — |

### 细节问题
- **导出限制 5000 条无前端提示**：注释写了"最多 5000 条"，但 UI 无任何超限提示或分批导出引导。
- **`operation_content` 为 JSON 字段**，前端直接原样展示（如 `{"query":"..."}`），未做可读性格式化。
- **无日志详情展开**：长内容被截断，无法点击查看完整操作 payload。
- **操作人角色为快照值**（`operator_role`），若角色后来改名，旧日志仍显示旧名——无"当时角色"标注。

---

## 九、告警待办队列 `/alarm/todo`

### 流程图对照
AI 识别处置流程图的下游部分（识别→分级→告警→建档→派单→处置）

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 多维度评分引警 | 后端 AI | — | — |
| 高/中/低风险分级通道 | ✅ 级别筛选 | ✅ | — |
| 全渠道/标准/仅日志告警 | `notify_method` 编码 | ❌ **推送未实现** | 无弹窗/声光/推送配置与投递 |
| 提取人员特征并建档 | ✅ 关联匿名人员 | ✅ | — |
| 自动打上风险多维标签 | 后端 | — | — |
| 是否纳入重点关注 | ✅ 标记关注按钮 | ⚠️ 仅逐条 | 无批量/无审核 |
| 告警进入待办队列 | ✅ 待办列表 | ✅ | — |
| 值班人工研判 | 确认/忽略/派单 | ✅ 三者都有 | ⚠️ 但见下方 |
| 真实/误报/存疑复核 | 忽略原因 | ⚠️ 选填 | 应强制 |
| 反馈/调度摄像头二次核验 | — | ❌ | 无"调度复核"入口 |
| 处置完毕/结案 | 工单详情+关闭 | ✅ | — |

### 细节问题
- **告警推送/消息中心完全缺失**：`fa_behavior_alert.notify_method` 已预留 `POPUP/SOUND_LIGHT/PUSH`，但前端无任何通知规则配置页、无消息中心、无实时提醒。
- **确认/忽略/派单后不自动刷新当前列表**：靠乐观更新改本地字段，需手动点"查询"才与后端一致。
- **派单"处理人"写死为 mock 两人**（张安全/李巡检），无按辖区/角色/在岗状态匹配的可派单人下拉。
- **误报忽略原因仅选填**（重要操作应强制备注）。
- **无批量确认/批量派单/批量忽略**。
- **工单无"转派/改派"功能**。
- **告警与工单两个 Tab 之间无联动高亮**（如某告警已派单，在告警 Tab 状态仍是"已确认"而非"已派单"）。
- **无"全部处理完成"的一键归档**。
- ✅ ~~告警与轨迹无联动~~ **P3 已完成**：告警列表每行支持"查看轨迹"弹窗

---

## 十、权限管理 `/system/index`

### 流程图对照
用户/权限管理流程图 → 权限自定义微调页面

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 展示完整权限树（菜单+按钮+数据） | ✅ 三级展示 | ✅ | — |
| 支持保存全新自定义权限模板复用 | ❌ | ❌ | 无"另存为模板"/"从模板加载" |
| 权限预览核对 | 保存前预览 | ❌ | 无"预览效果"按钮 |
| 提交保存 | ✅ | ✅ | — |
| 存储用户-权限绑定关系 | 后端 | — | — |
| 权限即时生效 | — | ❌ | 仅"重新登录生效" |
| 业务用户重新登录系统验证 | — | ⚠️ 登录有 | 但改完权限不自检 |

### 细节问题
- **保存后仅"重新登录生效"，无即时生效机制**；若管理员取消自身配置权限会自锁。
- **"全选子项"不联动父菜单勾选**：可能勾了按钮权限却看不到菜单。
- **无"重置/取消/还原默认"按钮**，改错只能重选。
- **超级管理员无任何可操作提示**（正确禁用，但可加一句"内置角色无需配置"说明）。
- **权限树仅支持两级嵌套**（菜单→按钮/数据），若未来加更深层级会丢失。

---

## 十一、角色管理 `/system/role`

### 流程图对照
用户/权限管理 → 用户类型管理（新增/修改/删除用户类型）

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 角色总览列表 | ✅ | ✅ | — |
| 新增角色（填写名称/安保/运维/图侦/交警） | ❌ | ❌ | **无新增角色入口** |
| 编辑已有角色（读取原有权限→修改） | 通过权限页间接实现 | ⚠️ 只能改权限 | 不能改名称/描述 |
| 删除用户类型（校验无账号引用→删） | ❌ | ❌ | **无删除角色入口** |
| 配置默认权限集 | ✅ 跳转权限页 | ✅ | — |
| 勾选权限（保安→仅摄像头查看...） | ✅ | ✅ | — |
| 保存 | ✅ | ✅ | — |

### 细节问题
- **无新增角色、无删除角色、无编辑角色名称/描述**：角色管理页本质是个"只读列表+跳转权限页"，不具备完整的 CRUD。
- **角色与账号的引用关系无前置校验提示**：删除角色时若有关联账号应在 UI 给出"X 个账号正在使用此角色"警告（目前靠后端 `RESTRICT` 外键报错）。
- **"内置"标签仅超级管理员有**，其他预置角色（安全负责人/安保等）无标识。

---

## 十二、账号管理 `/system/user`

### 流程图对照
用户/权限管理 → 用户账号管理（所有账号）

| 流程环节 | 设计 | 前端现状 | 缺失 |
|----------|------|----------|------|
| 账号列表（管理员/保安/运维/业务员） | ✅ | ✅ | — |
| 单据号操作（新增/编辑/启用/禁用/注销） | ⚠️ 有新增+启停 | ❌ **缺编辑/注销** | 见下方 |
| 新增用户/自助注册审核 | ✅ 新增有 | 注册在登录页 | — |
| 录入账号/姓名/部门/联系方式 | ✅ | ✅ | — |
| 下拉选择已创建用户类型 | ✅ | ✅ | — |
| 系统自动带入类型默认权限 | 后端 | — | — |
| 配置该类型默认权限集 | 间接触发 | — | — |
| 勾选权限（保安→仅摄像头查看...） | — | 此处在权限页 | — |
| 自定义手分配权限（覆盖类型默认） | ❌ | ❌ | **无单人自定义权限入口** |
| 保存 | ✅ | ✅ | — |
| 存储用户-权限绑定关系 | 后端 | — | — |
| 权限即时生效 | — | ❌ | 同上 |
| 业务用户登录系统验证 | ✅ | ✅ | — |
| 系统读取用户-绑定权限（类型默认+单人覆盖） | 后端 | — | — |
| 前端隐藏无权限菜单/展示完整权限树 | ✅ | ✅ | — |
| 正常访问/无权限弹窗拦截 | ✅ | ✅ | — |

### 细节问题
- **账号密码相关（核心缺口）**：
  - 列表不显示任何密码状态（是否已设置/是否过期/是否首次登录强制改密）
  - **无"修改密码/重置密码"入口**——账号创建后密码再也无法更改
  - 新增弹窗**无"确认密码"**
  - 密码仅非空校验、无复杂度要求、以明文经 API 传输
- **无"自定义单人权限"入口**：数据库设计了 `sys_account_permission` 表（账号独立权限优先覆盖角色权限），但前端完全没有"给这个账号单独配权限"的功能。
- **无删除账号入口**（对照物理删除设计，离职场景）。
- **保护逻辑无说明文案**：`account_id=1` 与自身账号不可禁用，但界面无 tooltip/禁用提示，易误以为按钮坏了。
- **无"最后登录 IP/登录次数/登录失败次数"展示**（`sys_account` 无这些字段，但审计场景通常需要）。

---

## 十三、数据管理首页 `/data/index`（P3 新增）

### 已实现功能

| 模块 | 内容 | 状态 |
|------|------|------|
| 总量统计 | 异常行为、轨迹链路、预警事件、处置工单、匿名人员、设备总数 | ✅ 已完成 |
| 类型分布 | 按异常行为类型统计横向条形图 | ✅ 已完成 |
| 区域分布 | 按设备所属区域统计横向条形图 | ✅ 已完成 |
| 趋势图表 | 近 7 天事件趋势柱状图 | ✅ 已完成 |

### 待增强项
- 分布图无点击下钻（如点击某类型应跳转异常行为记录并自动筛选）
- 无数据导出
- 无自定义时间范围选择

---

## 十四、新增后端接口（P3）

| 接口 | 功能 | 状态 |
|------|------|------|
| `GET /api/v1/behaviors?camera_id=` | 行为记录按设备筛选 | ✅ 已完成 |
| `GET /api/v1/behaviors/export` | 异常行为 CSV 导出 | ✅ 已完成 |
| `GET /api/v1/tracks/export` | 轨迹链路 CSV 导出 | ✅ 已完成 |
| `GET /api/v1/alerts/export` | 预警告警 CSV 导出 | ✅ 已完成 |
| `GET /api/v1/persons/{person_id}` | 人员详情（档案 + 最近 20 条行为） | ✅ 已完成 |
| `GET /api/v1/dashboard/data-index` | 数据管理首页聚合数据 | ✅ 已完成 |

---

## 十五、P3 已完成的前端组件

| 组件 | 功能 | 使用位置 |
|------|------|----------|
| `PersonDetailModal.vue` | 人员详情弹窗（档案 + 最近行为） | 行为记录页、轨迹查询页 |
| `DataIndexView.vue` | 数据管理首页（统计 + 分布 + 趋势） | `/data/index` 路由 |

---

## 十六、遗留问题汇总（按优先级）

### P0 - 核心功能缺失
1. **视频流接入**：实时轨迹、轨迹回放等核心场景无视频画面
2. **地图/热力可视化**：重点区域热力、轨迹地图回放完全缺失
3. **告警推送/消息中心**：notify_method 已预留但无实现
4. **账号密码管理**：无修改/重置密码功能

### P1 - 重要功能缺失
1. **设备远程控制**：无重启/开画面/调角度入口
2. **故障处置闭环**：缺派单/维修/复检全链路
3. **批量操作**：行为记录、告警、用户资料均无批量处理能力
4. **角色 CRUD**：无新增/删除/编辑角色功能
5. **单人权限自定义**：`sys_account_permission` 表设计未消费

### P2 - 体验优化
1. **数据导出限制提示**：5000 条限制无前端提示
2. **JSON 字段格式化**：操作日志内容可读性差
3. **权限即时生效**：改完权限需重新登录
4. **页面功能重叠**：轨迹记录与事后轨迹查询入口混淆
5. **区域筛选**：多页面 `region_code` 未暴露

### P3 - 已完成
- ✅ 数据联动（行为→轨迹/人员、告警→轨迹、轨迹→人员、设备→行为、看板→各模块）
- ✅ 数据导出（行为/轨迹/告警/日志 CSV 导出）
- ✅ 人员详情弹窗
- ✅ 数据管理首页
- ✅ 看板卡片点击跳转
- ✅ 设备行为记录查看
