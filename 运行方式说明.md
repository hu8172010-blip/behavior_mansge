# 行为管理系统运行方式说明

## 一、项目结构

本项目采用前后端分离结构：

- `frontend`：Vue 3 + Vite + TypeScript 前端项目
- `backend`：FastAPI 后端项目
- `recognition_model`：外部异常行为识别沙箱（anomaly_tracker），提供 `/api/jobs` 推理接口
- `infra`：Nginx、数据库等基础设施配置
- `docs`：系统架构设计文档

当前系统已完成从原型到全栈功能的升级，主要包含以下模块：

- **总览看板**：今日事件、待确认预警、活跃轨迹、设备在线率、事件趋势图、最新预警列表，支持点击卡片跳转对应模块
- **实时轨迹管理**：行进中轨迹实时列表、重点关注人员标记
- **事后轨迹查询**：按链路状态、时间范围、关键字检索，支持查看链路详情、人员详情及 CSV 导出
- **设备列表管理**：设备台账（新增 / 编辑 / 查询）、故障记录（查询 / 关闭）、设备行为记录查看
- **异常行为记录**：按类型、级别、状态、设备筛选，支持查看关联轨迹 / 人员及 CSV 导出
- **告警待办队列**：预警确认、误报忽略、派单处置、工单列表，支持查看轨迹及 CSV 导出
- **处置工单详情**：工单处理、关闭、回链异常行为与轨迹
- **数据管理**：数据总量统计、行为类型分布、区域分布、近 7 天趋势
- **用户资料管理**：匿名人员列表、重点关注标记、人员详情
- **系统操作日志**：模块 / 操作类型 / 关键字 / 时间检索，CSV 导出
- **系统管理**：账号管理、角色管理、权限配置
- **登录认证**：基于 JWT 的后端认证，角色权限控制菜单与操作
- **异常行为识别模型接入**：通过 `/inference/jobs` 上传视频，调用本地 `anomaly_tracker`（端口 8080）进行推理，并将结果写入业务表
- **模拟实验室**：临时/持久化两种模式，支持上传视频、沙箱推演、保存为 `lab_record`，并可发布到业务库

## 二、运行环境

建议安装以下环境：

- Node.js 18 或更高版本
- npm 9 或更高版本
- Python 3.10 或更高版本（运行后端时需要）
- MySQL、Redis（后端完整运行时需要）

检查 Node.js 和 npm 是否安装成功：

```powershell
node -v
npm -v
```

## 三、运行前端项目

### 方式一：从项目根目录运行

当前项目根目录为：

```text
D:\software\behavior_mansge
```

在项目根目录执行：

```powershell
npm --prefix .\frontend install --cache .\frontend\.npm-cache
npm --prefix .\frontend run dev
```

### 方式二：在 frontend 目录运行

将终端工作目录切换到：

```text
D:\software\behavior_mansge\frontend
```

然后执行：

```powershell
npm install --cache .npm-cache
npm run dev
```

启动成功后，终端通常会显示访问地址：

```text
http://localhost:5173
```

使用浏览器打开该地址即可访问前端系统页面。

## 四、前端生产构建

在项目根目录执行：

```powershell
npm --prefix .\frontend run build
```

或者在 `frontend` 目录执行：

```powershell
npm run build
```

构建成功后，会在 `frontend` 目录生成 `dist` 文件夹。

## 五、预览生产构建结果

先完成生产构建，再执行：

```powershell
npm --prefix .\frontend run preview
```

默认预览地址通常为：

```text
http://localhost:4173
```

## 六、运行后端项目

后端项目位于 `backend` 目录。首次运行时，建议创建 Python 虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

安装后端依赖：

```powershell
pip install -r .\backend\requirements.txt
```

配置环境变量。将 `backend\.env.example` 复制为 `backend\.env`，并根据本地 MySQL / Redis 实际地址修改：

```text
DATABASE_URL=mysql+asyncmy://app:app_password@localhost:3306/behavior_management
REDIS_URL=redis://localhost:6379/0
CORS_ORIGINS=http://localhost:5173
ANOMALY_TRACKER_URL=http://127.0.0.1:8080
```

> 如果后端依赖安装后出现 `ImportError: No module named 'multipart'`，说明缺少 `python-multipart`，已将其写入 `backend/requirements.txt`。

初始化数据库。当前后端使用 SQLAlchemy 模型，在 `backend` 目录执行以下命令创建数据表：

```powershell
python -c "from app.db.session import engine; from app.models.base import Base; import asyncio; from sqlalchemy.ext.asyncio import AsyncEngine; async def create(): async with engine.begin() as conn: await conn.run_sync(Base.metadata.create_all); asyncio.run(create())"
```

其中 `lab_record` 表用于模拟实验室持久化，在 `backend` 目录下也提供了专门的初始化脚本，可直接用 MySQL 客户端执行：

```powershell
mysql -u <用户名> -p <数据库名> < .\scripts\init_lab_record.sql
```

启动后端服务（推荐用 `python -m uvicorn` 方式，避免 Windows 下 uvicorn 命令不在 PATH 的问题）：

```powershell
python -m uvicorn app.main:app --reload --port 8000
```

如果你已经激活了虚拟环境，也可以直接使用：

```powershell
uvicorn app.main:app --reload --port 8000
```

后端启动后，API 地址通常为：

```text
http://localhost:8000
```

接口文档地址：

```text
http://localhost:8000/docs
```

如果项目后端入口文件或模块路径与上述命令不同，请以 `backend` 目录中的实际代码结构为准。

## 七、前后端联调

前端开发服务器已经配置了 `/api` 代理，默认将接口请求转发到：

```text
http://localhost:8000
```

因此联调时需要同时启动前端和后端：

- 前端：`http://localhost:5173`
- 后端：`http://localhost:8000`

建议打开两个终端窗口分别运行前后端服务。

## 八、常见问题

### 1. npm 报错找不到 package.json

如果出现以下错误：

```text
ENOENT: no such file or directory, open 'D:\software\behavior_mansge\package.json'
```

说明当前目录是项目根目录，但 `package.json` 位于 `frontend` 目录。请使用：

```powershell
npm --prefix .\frontend run dev
```

### 2. npm 报错 EPERM

如果 npm 缓存目录没有写入权限，可以使用项目目录下的缓存：

```powershell
npm install --cache .npm-cache
```

或者从项目根目录执行：

```powershell
npm --prefix .\frontend install --cache .\frontend\.npm-cache
```

### 3. 5173 端口被占用

关闭已经运行的 Vite 服务后重新启动。如果需要指定其他端口，可以执行：

```powershell
npm --prefix .\frontend run dev -- --port 5174
```

### 4. uvicorn 命令找不到

如果出现以下错误：

```text
uvicorn : 无法将"uvicorn"项识别为 cmdlet、函数、脚本文件或可运行程序的名称
```

说明 uvicorn 已安装，但可执行文件不在系统 PATH 中。请使用以下命令启动：

```powershell
python -m uvicorn app.main:app --reload --port 8000
```

### 5. 页面打开后没有接口数据

请确认后端服务已经启动，并检查后端是否监听在：

```text
http://localhost:8000
```

当前系统为前后端分离全栈应用，页面数据均来自后端接口。如果后端未启动，页面会显示加载失败或空数据提示。

## 九、推荐启动顺序

1. **启动 MySQL、Redis**（按本地部署方式启动即可，不依赖项目目录）。

2. **启动异常行为识别沙箱（端口 8080）**：

   在 `recognition_model` 目录执行：

   ```powershell
   cd D:\software\behavior_mansge\recognition_model
   $env:ANOMALY_CLASSIFIER_CHECKPOINT='training_runs\protected-v4-seed137\best.pth'
   .venv\Scripts\python.exe -m anomaly_tracker --host 0.0.0.0 --port 8080
   ```

3. **启动后端服务（端口 8000）**：

   在 `backend` 目录执行：

   ```powershell
   cd D:\software\behavior_mansge\backend
   python -m uvicorn app.main:app --reload --port 8000
   ```

4. **启动前端开发服务器（端口 5173 或 5174）**：

   在项目根目录执行：

   ```powershell
   cd D:\software\behavior_mansge
   npm --prefix .\frontend run dev
   ```

5. **浏览器访问**：

   ```text
   http://localhost:5173
   ```

   若 5173 被占用，终端会提示新的端口号（如 5174），按终端提示访问即可。

## 十、登录与角色演示

系统已接入后端 JWT 认证，登录页支持账号密码登录和注册。登录成功后，前端会根据后端返回的角色权限控制侧边栏菜单、页面路由和操作按钮。

默认演示账号：

| 角色 | 用户名 | 密码 | 权限特点 |
| --- | --- | --- | --- |
| 超级管理员 | `admin` | `admin123` | 全部功能权限，包括系统管理、数据导出、派单处置 |
| 园区安全负责人 | `security` | `sec123` | 查看园区安全监测、设备管理、告警处置 |
| 安保/巡检人员 | `guard` | `guard123` | 查看辖区预警、轨迹、工单处理 |
| 心理辅导员(EAP) | `counselor` | `coun123` | 查看行为监测、人员资料 |
| 部门主管 | `supervisor` | `sup123` | 查看本部门相关数据和报表 |
| 员工本人 | `employee` | `emp123` | 查看个人授权状态和个人相关数据 |
| 合规审计员 | `auditor` | `aud123` | 查看全量数据、操作日志、导出功能 |

注册页支持创建新账号，可选择安保/巡检人员、心理辅导员、部门主管或员工本人等角色。注册成功后可直接登录使用。

## 十一、新增功能说明（P3）

本次更新包含以下增强功能：

### 1. 数据联动
- **行为记录 → 轨迹 / 人员**：在异常行为记录列表中，点击“查看轨迹”或“查看人员”可弹出对应详情
- **告警 / 工单 → 轨迹**：在告警待办和工单列表中，点击“查看轨迹”可回链到轨迹链路详情
- **轨迹 → 人员**：在轨迹列表中，点击“查看人员”可查看人员档案和最近行为
- **设备 → 行为**：在设备台账中，点击“行为”可查看该设备最近检出的异常行为
- **看板 → 各模块**：点击总览看板顶部统计卡片可直接跳转到对应功能页面

### 2. 数据导出
以下页面支持按当前筛选条件导出 CSV 文件（最大 5000 条）：
- 异常行为记录：`/api/v1/behaviors/export`
- 轨迹链路：`/api/v1/tracks/export`
- 预警告警：`/api/v1/alerts/export`
- 系统操作日志：`/api/v1/logs/export`

### 3. 数据管理首页
`/data/index` 页面提供系统数据总览：
- 六大总量指标：异常行为、轨迹链路、预警事件、处置工单、匿名人员、设备总数
- 行为类型分布横向条形图
- 区域分布横向条形图
- 近 7 天事件趋势柱状图

### 4. 人员详情弹窗
点击任意“查看人员”按钮，可查看：
- 人员基础档案（外貌特征、首次 / 最后出现时间、匹配账号、风险等级）
- 行为次数、轨迹次数统计
- 最近 20 条行为记录（时间、类型、级别、状态、设备、区域）


## 十二、异常行为识别沙箱与模拟实验室

### 1. 识别沙箱 anomaly_tracker

anomaly_tracker 是独立的 YOLO Pose + BoT-SORT + R3D-18 推理服务，目录：

`	ext
D:\software\behavior_mansge\recognition_model
`

首次使用需创建 venv 并安装依赖：

`powershell
cd D:\software\behavior_mansge\recognition_model
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
`

确认以下文件存在：

`	ext
D:\software\behavior_mansge\recognition_model\yolo26n-pose.pt
D:\software\behavior_mansge\recognition_model\training_runs\protected-v4-seed137\best.pth
`

启动沙箱：

`powershell
cd D:\software\behavior_mansge\recognition_model
='training_runs\protected-v4-seed137\best.pth'
.venv\Scripts\python.exe -m anomaly_tracker --host 0.0.0.0 --port 8080
`

### 2. 业务闭环 /inference/jobs

后端 POST /api/v1/inference/jobs 会：

1. 接收上传的视频；
2. 调用 nomaly_tracker 推理；
3. 把识别结果写入业务表：异常行为、预警、工单、人员、轨迹。

接口需在 ackend/.env 中配置：

`	ext
ANOMALY_TRACKER_URL=http://127.0.0.1:8080
`

### 3. 模拟实验室 /lab

前端路径 /lab/simulation 支持两种模式：

- 临时模式：识别结果仅在前端展示，刷新后消失；
- 持久化模式：保存到 lab_record 表，可在 /lab/records 恢复、发布或删除。

持久化后点发布会把该记录写入业务表，等于一次手动业务闭环。

### 4. lab_record 表

lab_record 用于存放模拟实验持久化数据。可用 SQLAlchemy 创建，或直接用 MySQL 脚本：

`powershell
cd D:\software\behavior_mansge\backend
mysql -u root -p behavior_mansge < .\scripts\init_lab_record.sql
`

确认表存在后，即可在 /lab/simulation 上传视频并测试。
